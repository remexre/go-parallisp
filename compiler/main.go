package compiler

import (
	"errors"

	"remexre.xyz/go-parallisp/ast"
)

// Compile compiles a module.
func Compile(module *ast.Module) (string, error) {
	// TODO
	return "", errors.New("TODO compiler.Compile")
}
